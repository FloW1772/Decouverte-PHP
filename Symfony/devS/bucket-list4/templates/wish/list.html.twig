{% extends 'base.html.twig' %}

{% block title %}Liste des idées{% endblock %}

{% block body %}
    <div class="container my-5">
        <h1 class="mb-4 text-center text-primary">📌 Liste des choses à faire</h1>

        {% if wishes is not empty %}
            <div class="list-group shadow-sm">
                {% for wish in wishes %}
                    <a href="{{ path('app_wish_detail', {id: wish.id}) }}"
                       class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                        <span class="fw-bold">{{ wish.title }}</span>
                        <span class="badge bg-primary rounded-pill">Voir</span>
                    </a>
                {% endfor %}
            </div>
        {% else %}
            <div class="alert alert-warning text-center">
                Aucune idée publiée pour l’instant.
            </div>
        {% endif %}
    </div>
{% endblock %}
